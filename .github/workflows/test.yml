name: test
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    name: run tests
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@c2
        with:
          path: musicxml

      - name: test
        uses: borales/actions-yarn@v2.3.0
        with:
          cmd: yarn test --ci
