import { PartAbbreviationDisplay } from './PartAbbreviationDisplay';

describe('PartAbbreviationDisplay', () => {
  it('runs without crashing', () => {
    expect(PartAbbreviationDisplay).not.toThrow();
  });
});
