import { InvertedVerticalTurn } from './InvertedVerticalTurn';

describe('InvertedVerticalTurn', () => {
  it('runs without crashing', () => {
    expect(InvertedVerticalTurn).not.toThrow();
  });
});
